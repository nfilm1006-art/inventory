// ==================== SEKTOR 1: AUTHENTICATION (LOGIN SYSTEMS) ====================
const ADMIN_USERNAME = "Admin";
const ADMIN_PASSWORD = "gesnt123";

let isLoggedIn = localStorage.getItem("gudang_session") === "true";

function aturVisibilitasHalaman() {
  const loginScreen = document.getElementById("halamanLogin");
  const mainScreen = document.getElementById("aplikasiUtama");

  if (isLoggedIn) {
    loginScreen.classList.add("hidden");
    mainScreen.classList.remove("hidden");
    renderAplikasi(); 
  } else {
    loginScreen.classList.remove("hidden");
    mainScreen.classList.add("hidden");
  }
}

document.getElementById("formLogin").addEventListener("submit", (e) => {
  e.preventDefault();
  const user = document.getElementById("loginUsername").value;
  const pass = document.getElementById("loginPassword").value;
  const errMsg = document.getElementById("errorLogin");

  if (user === ADMIN_USERNAME && pass === ADMIN_PASSWORD) {
    isLoggedIn = true;
    localStorage.setItem("gudang_session", "true");
    errMsg.classList.add("hidden");
    e.target.reset();
    aturVisibilitasHalaman();
    catatAktivitas("🔑 Pengguna 'admin' berhasil login ke sistem.");
    simpanDanSiarkan();
  } else {
    errMsg.classList.remove("hidden");
  }
});

document.getElementById("btnLogout").addEventListener("click", () => {
  if (confirm("Apakah Anda ingin keluar dari sistem logistik?")) {
    isLoggedIn = false;
    localStorage.removeItem("gudang_session");
    aturVisibilitasHalaman();
  }
});

// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      <// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.          // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.        // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.      // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.    // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.  // ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.// ==================== SEKTOR 2: MANAGEMEN DATA GUDANG REAL-TIME ====================
const placeholderImg = "data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24' fill='none' stroke='%2364748b' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect width='18' height='18' x='3' y='3' rx='2' ry='2'/><circle cx='9' cy='9' r='2'/><path d='m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21'/></svg>";

const initialInventory = [
  { id: '1', nama: 'Kabel Belden Cat5', stok: 220, minimalStok: 10, rak: 'RAK A-1 Depo', foto: placeholderImg },
  { id: '2', nama: 'Mur 14', stok: 4, minimalStok: 12, rak: 'Rak B-2 501', foto: placeholderImg },
  { id: '3', nama: 'Cat semprot putih', stok: 30, minimalStok: 5, rak: 'Rak C-1 501', foto: placeholderImg },
];

// DATA BARU: Daftar lokasi rak default awal
const initialRacks = ['RAK A-1 Depo', 'Rak B-2 501', 'Rak C-1 501', 'Gudang Utama', 'Lainnya'];

let products = JSON.parse(localStorage.getItem('gudang_data_foto')) || initialInventory;
// Mengambil list rak dari localstorage jika sudah pernah disimpan
let listRak = JSON.parse(localStorage.getItem('gudang_list_rak')) || initialRacks;

let logs = JSON.parse(localStorage.getItem('gudang_logs')) || [
  { teks: "Sistem diinisialisasi berhasil.", waktu: `${new Date().toLocaleDateString('id-ID')} ${new Date().toLocaleTimeString('id-ID')}` }
];

const channel = new BroadcastChannel('gudang_realtime_total_channel');

function mainkanSuaraAlarm() {
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  oscillator.type = 'sine';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime); 
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.15);
}

function catatAktivitas(teks) {
  const tgl = new Date().toLocaleDateString('id-ID');
  const jam = new Date().toLocaleTimeString('id-ID');
  const logBaru = { teks: teks, waktu: `${tgl} ${jam}` };
  logs.unshift(logBaru); 
  if (logs.length > 30) logs.pop(); 
}

// LOGIKA BARU: Fungsi untuk merender pilihan opsi pada Dropdown Input & Dropdown Filter
function renderDropdownRak() {
  const inputRakSelect = document.getElementById('inputRak');
  const filterRakSelect = document.getElementById('filterRak');
  
  if (!inputRakSelect || !filterRakSelect) return;

  // Simpan nilai lama sebelum di-render ulang agar pilihan tidak loncat
  const selectedInputVal = inputRakSelect.value;
  const selectedFilterVal = filterRakSelect.value || 'SEMUA';

  // Render untuk pilihan di Input Form
  inputRakSelect.innerHTML = '';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    inputRakSelect.appendChild(opt);
  });
  if (listRak.includes(selectedInputVal)) inputRakSelect.value = selectedInputVal;

  // Render untuk pilihan di Filter Monitoring
  filterRakSelect.innerHTML = '<option value="SEMUA">✨ Tampilkan Semua Lokasi</option>';
  listRak.forEach(rak => {
    const opt = document.createElement('option');
    opt.value = rak;
    opt.innerText = rak;
    filterRakSelect.appendChild(opt);
  });
  filterRakSelect.value = selectedFilterVal;
}

function renderAplikasi() {
  if (!isLoggedIn) return; 

  const tabelBodi = document.getElementById('tabelBodi');
  const kontainerNotifikasi = document.getElementById('kontainerNotifikasi');
  const badgeAlert = document.getElementById('badgeAlert');
  const kontainerLog = document.getElementById('kontainerLog');
  const filterRak = document.getElementById('filterRak');
  
  if (!tabelBodi) return; 
  
  // Update isi dropdown terlebih dahulu sebelum memilah tabel
  renderDropdownRak();
  
  tabelBodi.innerHTML = '';
  kontainerNotifikasi.innerHTML = '';
  if (kontainerLog) kontainerLog.innerHTML = '';
  let jumlahAlert = 0;

  const lokasiDipilih = filterRak ? filterRak.value : 'SEMUA';
  
  products.forEach(item => {
    if (lokasiDipilih !== 'SEMUA' && item.rak !== lokasiDipilih) {
      return; 
    }

    const isKritis = Number(item.stok) <= Number(item.minimalStok);
    
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-slate-750 transition-colors border-b border-slate-700/50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-left whitespace-nowrap">
        <img src="${item.foto || placeholderImg}" alt="${item.nama}" class="w-10 h-10 md:w-12 md:h-12 object-cover rounded-lg bg-slate-900 border border-slate-700 shadow-inner" />
      </td>
      <td class="px-4 py-3 font-medium text-white text-left break-words">
        ${item.nama}
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="font-mono bg-slate-900 px-2.5 py-1 rounded text-slate-400 text-[11px] md:text-xs border border-slate-700/50">
          ${item.rak}
        </span>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <span class="px-3 py-1 rounded-full font-bold text-[11px] md:text-xs inline-block ${isKritis ? 'bg-red-500/20 text-red-400 border border-red-500/30' : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'}">
          ${item.stok} Pcs
        </span>
      </td>
      <td class="px-4 py-3 text-center text-slate-400 font-medium whitespace-nowrap">
        ${item.minimalStok} Pcs
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <div class="flex justify-center gap-1.5">
          <button data-id="${item.id}" data-aksi="kurang" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">-1</button>
          <button data-id="${item.id}" data-aksi="tambah" class="bg-slate-700 hover:bg-slate-600 active:scale-95 text-white text-xs px-2.5 py-1 rounded transition font-bold select-none">+1</button>
        </div>
      </td>
      <td class="px-4 py-3 text-center whitespace-nowrap">
        <button data-id="${item.id}" data-aksi="hapus" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white text-xs px-2.5 py-1.5 rounded transition border border-red-500/20 flex items-center justify-center mx-auto select-none">
          🗑️
        </button>
      </td>
    `;
    tabelBodi.appendChild(tr);

    if (isKritis) {
      jumlahAlert++;
      const alertDiv = document.createElement('div');
      alertDiv.className = 'bg-red-950/40 border-l-4 border-red-500 p-3 rounded-r-lg shadow-md border border-y-slate-700 border-r-slate-700 flex gap-3 items-center';
      alertDiv.innerHTML = `
        <img src="${item.foto || placeholderImg}" class="w-10 h-10 object-cover rounded bg-slate-900 border border-red-500/20" />
        <div>
          <p class="text-xs text-red-200 font-medium">⚠️ Stok ${item.nama} kritis! Sisa ${item.stok} Pcs</p>
          <span class="text-[9px] text-red-400 block mt-0.5 font-mono">LIVE MONITORING</span>
        </div>
      `;
      if (kontainerNotifikasi) kontainerNotifikasi.appendChild(alertDiv);
    }
  });

  if (badgeAlert) badgeAlert.innerText = `${jumlahAlert} Terdeteksi`;
  if (jumlahAlert === 0 && kontainerNotifikasi) {
    kontainerNotifikasi.innerHTML = `<div class="text-center text-slate-500 pt-10 text-sm"><p>✅ Stok aman terkendali.</p></div>`;
  }

  logs.forEach(log => {
    const logDiv = document.createElement('div');
    logDiv.className = 'flex justify-between items-start py-1 border-b border-slate-700/30 text-slate-300 hover:text-white text-xs';
    logDiv.innerHTML = `
      <span class="break-all pr-2">➡️ ${log.teks}</span>
      <span class="text-slate-500 shrink-0 text-[10px] font-mono">${log.waktu}</span>
    `;
    if (kontainerLog) kontainerLog.appendChild(logDiv);
  });
}

// SIMPAN & SIARKAN UPDATE (Ditambahkan sinkronisasi listRak)
function simpanDanSiarkan() {
  localStorage.setItem('gudang_data_foto', JSON.stringify(products));
  localStorage.setItem('gudang_logs', JSON.stringify(logs));
  localStorage.setItem('gudang_list_rak', JSON.stringify(listRak));
  renderAplikasi();
  channel.postMessage({ products, logs, listRak });
}

channel.onmessage = (event) => {
  products = event.data.products;
  logs = event.data.logs;
  if (event.data.listRak) listRak = event.data.listRak;
  renderAplikasi();
};

// HANDLER EVENT FILTER RAK
if (document.getElementById('filterRak')) {
  document.getElementById('filterRak').addEventListener('change', renderAplikasi);
}

// LOGIKA BARU: Menangani Tombol Kelola Rak (Tambah/Hapus Menu Dropdown)
document.getElementById('btnKelolaRak').addEventListener('click', () => {
  const menuOpsi = prompt(
    "⚙️ PENGATURAN DROPDOWN RAK\n\n" +
    "Ketik '1' = Untuk MENAMBAH Rak Baru\n" +
    "Ketik '2' = Untuk MENGHAPUS Rak yang Ada"
  );

  if (menuOpsi === '1') {
    const namaRakBaru = prompt("Masukkan nama lokasi rak baru:");
    if (namaRakBaru && namaRakBaru.trim() !== "") {
      const namaBersih = namaRakBaru.trim();
      if (listRak.includes(namaBersih)) {
        alert("⚠️ Nama rak tersebut sudah terdaftar!");
      } else {
        listRak.push(namaBersih);
        catatAktivitas(`Menambahkan lokasi rak baru ke sistem: "${namaBersih}"`);
        simpanDanSiarkan();
        alert(`✅ Sukses menambahkan "${namaBersih}" ke pilihan dropdown.`);
      }
    }
  } else if (menuOpsi === '2') {
    // Membuat teks list daftar rak saat ini untuk mempermudah penghapusan
    let daftarTeks = "Ketik nama rak yang ingin dihapus secara pas:\n";
    listRak.forEach((r, idx) => { daftarTeks += `${idx + 1}. ${r}\n`; });
    
    const targetHapus = prompt(daftarTeks);
    if (targetHapus && targetHapus.trim() !== "") {
      const namaHapus = targetHapus.trim();
      if (listRak.includes(namaHapus)) {
        listRak = listRak.filter(r => r !== namaHapus);
        catatAktivitas(`Menghapus lokasi rak "${namaHapus}" dari pilihan sistem`);
        simpanDanSiarkan();
        alert(`🗑️ "${namaHapus}" telah dihapus dari pilihan dropdown.`);
      } else {
        alert("❌ Nama rak tidak ditemukan atau salah ketik!");
      }
    }
  }
});

// SUBMIT FORM TAMBAH BARANG
document.getElementById('formBarang').addEventListener('submit', (e) => {
  e.preventDefault();
  const nama = document.getElementById('inputNama').value;
  const rak = document.getElementById('inputRak').value;
  const stok = Number(document.getElementById('inputStok').value);
  const minimalStok = Number(document.getElementById('inputMin').value);
  const fileFoto = document.getElementById('inputFoto').files[0];

  const buatBarangBaru = (fotoUrl) => {
    const barangBaru = { id: Date.now().toString(), nama, rak, stok, minimalStok, foto: fotoUrl };
    products.push(barangBaru);
    catatAktivitas(`Menambahkan item baru "${nama}" di [${rak}] (Stok: ${stok})`);
    if (stok <= minimalStok) mainkanSuaraAlarm();
    simpanDanSiarkan();
    e.target.reset();
  };

  if (fileFoto) {
    const reader = new FileReader();
    reader.onload = function(event) { buatBarangBaru(event.target.result); };
    reader.readAsDataURL(fileFoto);
  } else {
    buatBarangBaru(placeholderImg);
  }
});

// ... Sisa handler untuk 'click' tabel bodi dan cetak excel di Sektor 3 tetap sama di bawahnya.
// ==================== SEKTOR 3: EKSPOR EXCEL GENERATOR SYSTEM ====================
document.getElementById('btnDownloadExcel').addEventListener('click', () => {
  if (logs.length === 0) {
    alert("⚠️ Tidak ada data aktivitas untuk diunduh!");
    return;
  }

  const barisExcel = [
    ["WAKTU", "AKTIVITAS / PERUBAHAN SISTEM", "KETERANGAN PENGGUNAAN"] 
  ];

  logs.forEach(log => {
    let teksAktivitas = log.teks;
    let teksKeterangan = "-";

    const regexKet = /\[Ket:\s*(.*?)\]/;
    const cocok = teksAktivitas.match(regexKet);

    if (cocok) {
      teksKeterangan = cocok[1]; 
      teksAktivitas = teksAktivitas.replace(regexKet, "").trim(); 
    }

    barisExcel.push([log.waktu, teksAktivitas, teksKeterangan]);
  });

  const lembarKerja = XLSX.utils.aoa_to_sheet(barisExcel);
  const bukuKerja = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(bukuKerja, lembarKerja, "Log Aktivitas");

  lembarKerja["!cols"] = [
    { wch: 15 }, 
    { wch: 55 }, 
    { wch: 35 }  
  ];

  const namaFile = `Log_Gudang_${new Date().toISOString().slice(0,10)}.xlsx`;
  XLSX.writeFile(bukuKerja, namaFile);
});

aturVisibilitasHalaman();